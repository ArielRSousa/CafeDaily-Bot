services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db

  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      - mongo

volumes:
  mongo_data:
